// let btn = document.getElementById("btn")
// btn.addEventListener("click", capitalizer())

console.log(name[0])
function capitalizer(){
    let name=document.getElementById("name").value;
    // let name="raju";
    if (name[0] != name[0].toUpperCase()){
        document.write(name[0].toUpperCase()+name.slice(1,))}
    else{
       document.write(name)
    }
}